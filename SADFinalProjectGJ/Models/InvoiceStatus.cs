namespace SADFinalProjectGJ.Models
{
    // Enum for Invoice Status
    public enum InvoiceStatus
    {
        Draft,      // Draft
        Sent,       // Sent
        Paid,       // Paid
        Overdue,    // Overdue
        Cancelled   // Cancelled
    }
}